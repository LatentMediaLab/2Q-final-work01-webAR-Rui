{
  "name": "fav-collection",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build && node scripts/clean-private-build-artifacts.mjs",
    "preview": "vite preview",
    "generate:placeholders": "node scripts/generate-placeholders.mjs",
    "generate:custom": "./generate-custom-posts.command",
    "cloudflare:dev": "wrangler dev --config wrangler.jsonc --env staging --local",
    "cloudflare:token:init": "node scripts/initialize-cloudflare-token.mjs",
    "cloudflare:test": "vitest run tests/CloudflareWorker.test.ts",
    "test": "vitest run",
    "lint": "eslint .",
    "typecheck": "tsc -b --pretty false"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/three": "^0.185.1",
    "eslint": "^10.7.0",
    "globals": "^17.7.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.64.0",
    "vite": "^8.1.4",
    "vitest": "^4.1.10",
    "wrangler": "4.127.1"
  }
}
